# See /usr/share/postfix/main.cf.dist for a commented, more complete version

# Debian specific:  Specifying a file name will cause the first
# line of that file to be used as the name.  The Debian default
# is /etc/mailname.
#myorigin = /etc/mailname

smtpd_banner = $myhostname ESMTP $mail_name (Ubuntu)
biff = no

# appending .domain is the MUA's job.
append_dot_mydomain = no

# Uncomment the next line to generate "delayed mail" warnings
#delay_warning_time = 4h

readme_directory = no

# See http://www.postfix.org/COMPATIBILITY_README.html -- default to 3.6 on
# fresh installs.
compatibility_level = 3.6

# TLS parameters
smtpd_tls_cert_file=/etc/ssl/certs/ssl-cert-snakeoil.pem
smtpd_tls_key_file=/etc/ssl/private/ssl-cert-snakeoil.key
smtpd_tls_security_level=may

smtp_tls_CApath=/etc/ssl/certs
smtp_tls_security_level=may
smtp_tls_session_cache_database = btree:${data_directory}/smtp_scache

smtpd_relay_restrictions = permit_mynetworks permit_sasl_authenticated defer_unauth_destination
myhostname = ubuntuwebserver
alias_maps = hash:/etc/aliases
alias_database = hash:/etc/aliases
mydestination = $myhostname, 103fmlineup, ubuntuwebserver, localhost.localdomain, localhost

# Network configuration
inet_interfaces = all
inet_protocols = ipv4
mynetworks = 127.0.0.0/8, 192.168.0.0/16, 10.0.0.0/8
mynetworks_style = subnet

# DNS configuration
disable_dns_lookups = no
smtp_host_lookup = dns, native

# Delivery retry settings
maximal_queue_lifetime = 5d
bounce_queue_lifetime = 5d
notify_classes = resource, software, bounce, delay, policy, protocol

# TLS settings for external delivery
smtp_tls_CAfile = /etc/ssl/certs/ca-certificates.crt

# Force TLS for external delivery
smtp_tls_mandatory_protocols = !SSLv2, !SSLv3, !TLSv1, !TLSv1.1
smtp_tls_mandatory_ciphers = medium

# Mailbox settings
mailbox_size_limit = 0
recipient_delimiter = +

# Logging configuration
maillog_file = /var/log/mail.log

# Use submission port (587) for external delivery
smtp_tls_security_level = may
smtp_tls_CAfile = /etc/ssl/certs/ca-certificates.crt
smtp_tls_session_cache_database = btree:${data_directory}/smtp_scache

# Configure to use submission port for external domains
smtp_tls_policy_maps = hash:/etc/postfix/tls_policy
smtp_tls_security_level = may 